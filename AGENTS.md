# Ambient Score プロジェクト — Codex向け文脈メモ

このプロジェクトの文脈メモは **`CLAUDE.md` を単一の正（source of truth）** として管理しています。
Codex／その他のエージェントも、プロジェクト概要・アプリ一覧（AS1/AA1）・技術的決定事項・
開発方針は **[`CLAUDE.md`](./CLAUDE.md) を参照**してください。

内容を更新する場合は `CLAUDE.md` を編集します（本ファイルは重複管理を避けるためのポインタです）。

## 最低限の要点

- **AS1** (`ambient-score.html`): 単一HTMLファイル・依存ライブラリなし・オフライン動作が設計の要。ビルドステップや外部依存を追加しないこと。
- **VADのファン誤検知対策 (v2.7.1〜)**: 声固有の指標（pitch/harmonic）を重視する重み・しきい値・活動ゲートに戻してある。挙動を不用意に崩さないこと。
- 詳細・背景・履歴はすべて `CLAUDE.md` にある。
